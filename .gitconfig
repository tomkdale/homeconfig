[alias]
	launch = !git add . && git commit -m. && git push
	source = config --get remote.origin.url
	co = checkout
  bomb = reset HEAD --hard

[user]
	email = tom.k.dale@gmail.com
	name = Tom
[init]
	defaultBranch = main

[core]
    excludesfile = ~/.gitignore
